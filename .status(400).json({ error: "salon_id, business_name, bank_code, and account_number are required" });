[33mcommit 3947e65464524a0c13c38a822a095c86378ca7db[m
Author: samjiboye <samjiboye01@gmail.com>
Date:   Thu Jul 23 00:51:20 2026 +0100

    Remove old payment connection routes

[1mdiff --git a/backend/routes/payments.js b/backend/routes/payments.js[m
[1mindex 03c7e13..245dbb6 100644[m
[1m--- a/backend/routes/payments.js[m
[1m+++ b/backend/routes/payments.js[m
[36m@@ -33,88 +33,4 @@[m [mrouter.get("/resolve-account", requireAuth, requireRole("owner"), async (req, re[m
   }[m
 });[m
 [m
[31m-router.post("/connect", requireAuth, requireRole("owner"), async (req, res) => {[m
[31m-  const { salon_id, business_name, bank_code, account_number } = req.body;[m
[31m-  if (!salon_id || !business_name || !bank_code || !account_number) {[m
[31m-    return res.status(400).json({ error: "salon_id, business_name, bank_code, and account_number are required" });[m
[31m-  }[m
[31m-  try {[m
[31m-    const { rows: salonRows } = await db.query("SELECT * FROM salons WHERE id = $1", [salon_id]);[m
[31m-    const salon = salonRows[0];[m
[31m-    if (!salon) return res.status(404).json({ error: "Salon not found" });[m
[31m-    if (salon.owner_id !== req.user.id) return res.status(403).json({ error: "Not your salon" });[m
[31m-[m
[31m-    const subaccount = await paystack.post("/subaccount", {[m
[31m-      business_name,[m
[31m-      bank_code,[m
[31m-      account_number,[m
[31m-      percentage_charge: COMMISSION_RATE * 100,[m
[31m-    });[m
[31m-[m
[31m-    await db.query([m
[31m-      "UPDATE salons SET paystack_subaccount_code = $1, paystack_payouts_enabled = 1 WHERE id = $2",[m
[31m-      [subaccount.subaccount_code, salon.id][m
[31m-    );[m
[31m-[m
[31m-    res.json({ ok: true, subaccount_code: subaccount.subaccount_code, account_name: subaccount.account_name });[m
[31m-  } catch (err) {[m
[31m-    console.error(err);[m
[31m-    res.status(400).json({ error: err.message || "Couldn't set up payouts for this salon." });[m
[31m-  }[m
[31m-});[m
[31m-[m
[31m-// GET /payments/connect/status?salon_id=[m
[31m-router.get("/connect/status", requireAuth, requireRole("owner"), async (req, res) => {[m
[31m-  const { salon_id } = req.query;[m
[31m-  try {[m
[31m-    const { rows } = await db.query("SELECT * FROM salons WHERE id = $1", [salon_id]);[m
[31m-    const salon = rows[0];[m
[31m-    if (!salon) return res.status(404).json({ error: "Salon not found" });[m
[31m-    res.json({ payoutsEnabled: !!salon.paystack_payouts_enabled });[m
[31m-  } catch (err) {[m
[31m-    console.error(err);[m
[31m-    res.status(500).json({ error: "Couldn't check payout status." });[m
[31m-  }[m
[31m-});[m
[31m-[m
[31m-router.post("/checkout", requireAuth, async (req, res) => {[m
[31m-  const { salon_id, service_id, time_slot } = req.body;[m
[31m-  if (!salon_id || !service_id || !time_slot) {[m
[31m-    return res.status(400).json({ error: "salon_id, service_id, and time_slot are required" });[m
[31m-  }[m
[31m-  try {[m
[31m-    const { rows: salonRows } = await db.query("SELECT * FROM salons WHERE id = $1", [salon_id]);[m
[31m-    const salon = salonRows[0];[m
[31m-    const { rows: serviceRows } = await db.query([m
[31m-      "SELECT * FROM services WHERE id = $1 AND salon_id = $2",[m
[31m-      [service_id, salon_id][m
[31m-    );[m
[31m-    const service = serviceRows[0];[m
[31m-    if (!salon || !service) return res.status(404).json({ error: "Salon or service not found" });[m
[31m-    if (!salon.paystack_subaccount_code || !salon.paystack_payouts_enabled) {[m
[31m-      return res.status(400).json({ error: "This salon hasn't finished setting up payouts yet." });[m
[31m-    }[m
[31m-[m
[31m-    const commission_amount = Math.round(service.price * COMMISSION_RATE * 100) / 100;[m
[31m-    const payout_amount = Math.round((service.price - commission_amount) * 100) / 100;[m
[31m-    const total = service.price + BOOKING_FEE;[m
[31m-[m
[31m-    const { rows: bookingRows } = await db.query([m
[31m-      `INSERT INTO bookings[m
[31m-        (customer_id, salon_id, service_id, time_slot, status, service_price, booking_fee, commission_rate, commission_amount, payout_amount, payment_status)[m
[31m-       VALUES ($1, $2, $3, $4, 'pending', $5, $6, $7, $8, $9, 'unpaid') RETURNING id`,[m
[31m-      [req.user.id, salon_id, service_id, time_slot, service.price, BOOKING_FEE, COMMISSION_RATE, commission_amount, payout_amount][m
[31m-    );[m
[31m-    const bookingId = bookingRows[0].id;[m
[31m-[m
[31m-    try {[m
[31m-      const transaction = await paystack.post("/transaction/initialize", {[m
[31m-        email: req.user.email,[m
[31m-        amount: Math.round(total * 100),[m
[31m-        currency: CURRENCY,[m
[31m-        subaccount: salon.paystack_subaccount_code,[m
[31m-        transaction_charge: Math.round((commission_amount + BOOKING_FEE) * 100),[m
[31m-        bearer: "subaccount",[m
[31m-        metadata: { booking_id: bookingId },[m
[31m-        callback_url: `${FRONTEND_URL}/?booking_success=1&booking_id=${bookingId}`,[m
[31m-      });[m
[32m+[m[32mrouter.post("/connect", requireAuth, requireRole("owner"),[m
